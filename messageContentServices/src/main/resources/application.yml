
server:
  port: 8903
spring:
  application:
    name: messagecontent    # 项目名称尽量用小写
eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8900/eureka/,http://localhost:8901/eureka    # 指定注册中心的地址
  instance:
    preferIpAddress: true